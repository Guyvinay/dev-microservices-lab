spring:
  application:
    name: dev-integration
server:
  port: 8002
  servlet.context-path: /dev-integration

################################################################################
# ELASTICSEARCH (Custom index settings)
################################################################################
elastic:
  index:
    private: es_private_message_index
    group: es_group_message_index
    email: email_index

rmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
  addresses: localhost:15672